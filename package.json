{
  "name": "command-palette",
  "version": "0.20.0",
  "main": "./lib/command-palette-view",
  "description": "Find and run available commands using `cmd-shift-p`.",
  "activationEvents": "command-palette:toggle",
  "repository": "https://github.com/atom/command-palette",
  "license": "MIT",
  "engines": {
    "atom": "*"
  },
  "dependencies": {
    "underscore-plus": "1.x"
  }
}
